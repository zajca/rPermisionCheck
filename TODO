--implemet level option
--control input
--make all argument mandatory

BUGS:
--can't check permission for folder/file with blank space in name
--can't handle unexisted files/folders declared in permission.json
--don't check hiden files (.something)
